version: "3.8"
services:
  integratedproject:
    image: penbeaa/integratedproject:latest
    build: .
    container_name: integratedproject
    ports:
      - "80:4000"
    environment:
       - PORT=4000
    volumes:
      - ./:/app
    command: ["node", "app.js"]
